# Deprecated: use repository root `.env` / `.env.example` as global config.
# This file is kept only for compatibility.
LISTEN_ADDR=:8090
JWT_SECRET=easyssl-dev-secret
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=postgres
PG_DATABASE=easyssl
PG_SSLMODE=disable
